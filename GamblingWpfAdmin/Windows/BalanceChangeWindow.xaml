<Window x:Class="GamblingWpfAdmin.Windows.BalanceChangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GamblingWpfAdmin.Windows"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="Изменение баланса" Height="300" Width="800">
    <Grid>
        <Border
            CornerRadius="25"
            BorderThickness="3" 
            BorderBrush="Black" 
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <StackPanel 
                Margin="15"
                Width="650"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="207*"/>
                        <ColumnDefinition Width="433*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Style="{StaticResource WelcomePageTextBlock}" Height="33" Width="192">
                    Текущий баланс:
                    </TextBlock>
                    <TextBox  Grid.Column="1" x:Name="Balance" Style="{StaticResource WelcomePageTextBox}" HorizontalAlignment="Stretch" Height="35" Width="400"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Content="0" Grid.Column="0" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="-1000" Grid.Column="1" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="-500" Grid.Column="2" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="-100" Grid.Column="3" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="+100" Grid.Column="4" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="+500" Grid.Column="5" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="+1000" Grid.Column="6" Style="{StaticResource WelcomePageButton}"/>
                    <Button Content="сброс" Grid.Column="7" Style="{StaticResource WelcomePageButton}"/>
                </Grid>
                <Button Content="Сохранить" Style="{StaticResource WelcomePageButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
