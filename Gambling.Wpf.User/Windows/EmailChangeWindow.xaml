<Window x:Class="Gambling.Wpf.User.Windows.EmailChangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:easy="clr-namespace:EasyCaptcha.Wpf;assembly=EasyCaptcha.Wpf"
        xmlns:local="clr-namespace:Gambling.Wpf.User.Windows"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        Loaded="Window_Loaded"
        Title="Смена электронной почты"
        Height="400" Width="700">
    <Grid>
        <Border
    CornerRadius="25"
    BorderThickness="3" 
    BorderBrush="Black" 
    HorizontalAlignment="Center"
    VerticalAlignment="Center">
            <StackPanel 
        Margin="15"
        Width="550"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
                <Grid Style="{StaticResource WelcomePageGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock 
Text="Старая почта"
Style="{StaticResource WelcomePageTextBlock}"/>
                    <TextBox
Name="OldEmail" 
Grid.Column="1"
Style="{StaticResource WelcomePageTextBox}"/>
                </Grid>
                <Grid Style="{StaticResource WelcomePageGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock 
Text="Новая почта"
Style="{StaticResource WelcomePageTextBlock}"/>
                    <TextBox 
Name="NewEmail" 
Grid.Column="1"
Style="{StaticResource WelcomePageTextBox}"/>
                </Grid>
                <Border
            CornerRadius="10"
            BorderThickness="2" 
            BorderBrush="Black" 
            Margin="0,5,0,5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
                    <easy:Captcha x:Name="CaptchaPlace" Margin="5" FontSize="25" HorizontalAlignment="Center"/>
                </Border>
                <Grid Style="{StaticResource WelcomePageGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                Text="Капча"
                Style="{StaticResource WelcomePageTextBlock}"/>
                    <TextBox 
                Name="CaptchaAnswer" 
                Grid.Column="1"
                Style="{StaticResource WelcomePageTextBox}"/>
                </Grid>
                <Button 
            Grid.Row="0" 
            x:Name="Authorization" 
            Content="Сменить электронную почту" 
            Style="{StaticResource WelcomePageButton}" Click="Authorization_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
